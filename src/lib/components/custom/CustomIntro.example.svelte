<!--
  Enable:
  - To use the custom intro, make sure the file path is
    src/lib/components/custom/CustomIntro.svelte
  - If you used the setup scripts, the custom intro should
    already be at the correct path.
  - For a manual installation, copy the content of
    CustomIntro.example.svelte into CustomIntro.svelte

  Environment:
  - Make sure the custom intro is enabled in the .env file:
    PUBLIC_ENABLE_CUSTOM_INTRO=true
  - You can optionally disable the default intro in the .env file:
    PUBLIC_ENABLE_DEFAULT_INTRO=false

  Updates:
  - CustomIntro.svelte is included in .gitignore, so this file
    stays persistent even if you update the code to a new version,
    e.g. with 'git pull'.
-->
<script lang="ts">
  // You can change the code below

  // You can change the code above
</script>

<div>
  <!-- You can change the code below -->

  <!-- You can change the code above -->
</div>
