<!--
  Enable:
  - To use the custom contacts, make sure the file path is
    src/lib/components/custom/CustomContacts.svelte
  - If you used the setup scripts, the custom contacts should
    already be at the correct path.
  - For a manual installation, copy the content of
    CustomContacts.example.svelte into CustomContacts.svelte

  Environment:
  - Make sure the custom contacts are enabled in the .env file:
    PUBLIC_ENABLE_CUSTOM_CONTACTS=true
  - You can optionally disable the default contacts in the .env file:
    PUBLIC_ENABLE_DEFAULT_CONTACTS=false

  Updates:
  - CustomContacts.svelte is included in .gitignore, so this file
    stays persistent even if you update the code to a new version,
    e.g. with 'git pull'.
-->
<script lang="ts">
  // You can change the code below

  // You can change the code above
</script>

<div>
  <!-- You can change the code below -->

  <!-- You can change the code above -->
</div>
